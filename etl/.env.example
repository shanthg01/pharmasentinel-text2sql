# Copy this file to `.env` in the same directory and fill in real values.
# These are read by etl/common/db.py via python-dotenv.

PGHOST=localhost
PGPORT=5439
PGDATABASE=pharmasentinel
PGUSER=postgres
PGPASSWORD=changeme
