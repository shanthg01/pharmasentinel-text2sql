export default function EvaluationTab() {
  return (
    <div>
      <h2>Evaluation</h2>
      <p>
        Runs and results from the gold-case eval suite (scripts/eval) — pass
        rate by category and split, and the test-split promotion gate — not
        yet implemented.
      </p>
    </div>
  );
}
