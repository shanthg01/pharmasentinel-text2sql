export default function CohortBuilderTab() {
  return (
    <div>
      <h2>Cohort Builder</h2>
      <p>
        Structured, form-driven cohort definition (drug, event, date range,
        seriousness, trial phase) that compiles to a validated Tier 3 query
        without requiring free-text input — not yet implemented.
      </p>
    </div>
  );
}
